# Disabled — out of EAS credits, builds are run manually via Xcode for now.
# name: Preview Build
#
# on:
#   push:
#     branches:
#       - main
#
# jobs:
#   build_ios:
#     type: build
#     params:
#       platform: ios
#       profile: preview
